import {InitializationAPPthunk} from "../../../data/InitializatonReducer";

export type T_MSTP_App ={
    inition  : boolean
    IsLogined : boolean
}

export type T_MDTP_App ={
    InitializationAPPthunk : typeof InitializationAPPthunk
}
export type TAppOwnProps ={

}